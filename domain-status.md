# daily-ainews.com 通信状況まとめ

最終確認日: 2026-07-18

## 現在の状態: すべて正常 ✅

| 項目 | 状態 |
|---|---|
| カスタムドメイン | `daily-ainews.com` — GitHub Pages 設定に登録済み |
| DNS (A レコード) | GitHub Pages の正規 IP (185.199.108〜111.153) に解決 |
| DNS (www) | `www.daily-ainews.com` も正常に解決 |
| HTTPS 証明書 | 発行済み (`approved`)。apex と www の両方をカバー。有効期限 2026-10-16 (GitHub が自動更新) |
| HTTP → HTTPS リダイレクト | 有効 (`https_enforced: true`、301 リダイレクト確認済み) |
| ネームサーバー | `01〜04.dnsv.jp` (お名前.com 系) |

## 構成のポイント

- デプロイは GitHub Actions 方式 (`build_type: workflow`) のため、**リポジトリに CNAME ファイルは不要**。カスタムドメインは GitHub のリポジトリ設定側 (Settings → Pages) に保存されている。CNAME ファイルが自動生成されるのはブランチデプロイ方式の場合のみ。
- DNS 設定はお名前.com 側の管理。A レコード 4 本 (185.199.108.153 / 109.153 / 110.153 / 111.153) と www の CNAME が設定済み。
- 妨げになる AAAA レコードは無し。

## 対応履歴 (2026-07-18)

1. **問題発見**: HTTPS 証明書が未発行 (`https_certificate: null`) で、`https://daily-ainews.com` が証明書エラーになっていた。DNS 自体は正常だったため、ドメイン設定時に証明書発行が開始されなかったことが原因と判断。
2. **修正**: GitHub 公式の復旧手順どおり、カスタムドメインを一度外して即座に再設定し、証明書発行を再トリガー。数分で発行完了。
3. **HTTPS 強制を有効化**: `https_enforced: true` に設定。HTTP アクセスは 301 で HTTPS にリダイレクトされる。
4. **DNS チェッカーで日本が赤 × だった件**: チェッカー側ノードの一時的なタイムアウト/キャッシュが原因。国内リゾルバ含む複数の DNS で正常解決を確認済みで、実害なし (解消済み)。
5. **ブラウザで http:// がリダイレクトされなかった件**: 反映待ち + ブラウザキャッシュが原因。現在は解消済み。

## 今後のメンテナンス

- 証明書の更新は GitHub が自動で行うため、**定期作業は不要**。
- お名前.com 側で DNS レコードやネームサーバーを変更すると証明書が無効になるので注意。
- ドメインの利用権を保護したい場合は、GitHub の「verified domain」設定 (Settings → Pages → Verify) を追加で行うとよい。
